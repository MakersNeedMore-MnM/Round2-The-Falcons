# Phase 36 — Mobile/PWA readiness

Cascadia now provides an installable web-app shell. API requests are excluded from its offline cache, so the app never shows cached telemetry or incidents as live data.
