# Phase 41 — Mobile operator companion

The console uses a compact bottom navigation on phones, hides dense secondary labels, enlarges touch controls, and prevents mobile browser zoom from shrinking form usability.

The PWA caches only its interface shell. Authentication, API data, incidents, telemetry, and decisions remain online-only; there is no offline operational-data cache and no autonomous acknowledgement.
