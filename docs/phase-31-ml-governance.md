# Phase 31 — ML governance

`GET /api/v1/ml-governance` presents stored detection-model versions, training-event counts, thresholds, creation and review counts, and analyst dispositions.

The report does not label analyst dispositions as accuracy metrics. It does not claim recall, precision, false-positive rate, fairness, or vendor model performance. Every model continues to require human review and every response action remains disabled.
